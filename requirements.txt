numpy>=1.26
opencv-python>=4.9
PyYAML>=6.0
pytest>=8.0
ruff>=0.5
mypy>=1.10
rerun-sdk>=0.34

# Only needed for src/house_builder/sync_checkpoint.py (pulling a trained checkpoint
# down from a Modal Volume). Not needed to run the harness itself.
modal>=1.0
